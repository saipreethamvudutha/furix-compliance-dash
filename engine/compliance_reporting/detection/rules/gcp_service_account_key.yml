title: GCP Service Account Key Created
id: furix-0050-gcp-sa-key
status: stable
description: A service-account key was created — long-lived credential, potential persistence.
logsource:
  service: gcp
detection:
  sel:
    _raw|contains: 'CreateServiceAccountKey'
  condition: sel
level: medium
tags: [attack.persistence, attack.t1098.001]
